name: Testing

on: [push]

jobs:
  prepare:
    runs-on: ubuntu-latest
    container: 
      image: docker
    steps:
      - run: git clone https://github.com/tanutapi/simple-meteor-chat.git
      - run: cd simple-meteor-chat
      - run: docker-compose up
    
  test:

    runs-on: ubuntu-latest

    container:
      image:  google/dart:latest

    steps:
    - uses: actions/checkout@v1
    - name: Install dependencies
      run: pub get
    - name: Run tests
      run: pub run test
